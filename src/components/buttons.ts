import { ButtonBuilder, ButtonStyle } from "discord.js";

export const tintiroBtn = new ButtonBuilder()
    .setCustomId("tintiro")
    .setLabel("🎲チンチロ🎲")
    .setStyle(ButtonStyle.Success);
